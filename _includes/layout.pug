
-function prettyDate(dateString){
    -var date = new Date(dateString);
    -var monthNames = [ 'January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December' ];
    -var m = monthNames[date.getMonth()];
    -var y = date.getFullYear();
    -var d = date.getDate();
    -return m+' '+d+', '+y;
-}

doctype html
html(lang="en")
    head
        block title
            title #{title}
        meta(name='description', content=`${metaDescription}`)
        meta(name='viewport', content='width=device-width, initial-scale=1')
        link(rel='stylesheet', href='/css/site.css')
        link(rel='stylesheet', href='//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.9.0/styles/default.min.css')
        link(rel='stylesheet', href='//fonts.googleapis.com/css?family=Khula|Merriweather') 
        link(rel='stylesheet', href='//maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css') 
        script(src='https://identity.netlify.com/v1/netlify-identity-widget.js')

    body
        script.
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

            //- ga('create', 'UA-????-1', 'auto');
            //- ga('send', 'pageview');
        #site
            nav#header
                ul
                    li
                        a.navbar-brand(href='/workshops') Workshops
                    li
                        a.navbar-brand(href='/blog') Blog
                    li
                        a.navbar-brand(href='/about') About
                    li
                        a.navbar-brand(href='/contact') Contact
            #sidebar
                include /sidebar.pug
            #content
                block content
                    div !{_layoutContent}
            #footer
                h6 &copy; Stephanie Hicks 2018
        script(src='//cdn.jsdelivr.net/npm/vue@2.5.16/dist/vue.js')
        script(src='//cdnjs.cloudflare.com/ajax/libs/axios/0.18.0/axios.min.js')
        script(src='//cdnjs.cloudflare.com/ajax/libs/date-fns/1.29.0/date_fns.min.js')
        script(src='/static/js/schedule.js')
        script(src='//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.12.0/highlight.min.js')
        script.
            hljs.initHighlightingOnLoad();
        script.
            if (window.netlifyIdentity) {
              window.netlifyIdentity.on("init", user => {
                if (!user) {
                  window.netlifyIdentity.on("login", () => {
                    document.location.href = "/admin/";
                  });
                }
              });
            }
